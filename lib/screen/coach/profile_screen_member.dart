import 'package:flutter/material.dart';

class ProfileScreenCoach extends StatelessWidget {
  const ProfileScreenCoach({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome to the Coach Profile Page!'),
    );
  }
}
