import 'package:flutter/material.dart';

class BookingScreenCoach extends StatelessWidget {
  const BookingScreenCoach({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome to the Coach Booking Page!'),
    );
  }
}
