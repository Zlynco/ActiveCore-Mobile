import 'package:flutter/material.dart';

class BookingScreenMember extends StatelessWidget {
  const BookingScreenMember({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome to the Booking Page!'),
    );
  }
}
