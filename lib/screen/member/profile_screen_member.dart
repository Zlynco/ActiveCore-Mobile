import 'package:flutter/material.dart';

class ProfileScreenMember extends StatelessWidget {
  const ProfileScreenMember({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome to the Profile Page!'),
    );
  }
}
