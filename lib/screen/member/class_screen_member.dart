import 'package:flutter/material.dart';

class ClassScreenMember extends StatelessWidget {
  const ClassScreenMember({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome to the Class Page!'),
    );
  }
}
